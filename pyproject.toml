[project]
name = "vitacall"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyspark>=3.5.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
    "requests>=2.32.0",
    "scikit-learn>=1.4.0",
]

[tool.setuptools.packages.find]
include = ["pipeline*"]
